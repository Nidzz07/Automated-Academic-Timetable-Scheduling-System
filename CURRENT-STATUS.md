# CURRENT-STATUS.md — Chronos

> Generated by inspection of the working tree on 2026-09-24. This file reports **what exists**,
> not what is planned. Anything described as a stub is a stub. Where a fact could not be
> verified by reading a file, it says "unknown".

---

## 1. Directory tree

Depth 3, excluding `node_modules/`, `.venv/`, `__pycache__/`, `.git/`, `dist/`.

```
.
├── .dockerignore                    file
├── .env.example                     file
├── .gitignore                       file
├── CLAUDE.md                        file
├── CONTEXT.md                       file (untracked)
├── CURRENT-PROGRESS.md              file (untracked)
├── ROADMAP.md                       file (untracked)
├── docker-compose.yml               file
├── requirements.txt                 file
├── .pytest_cache/                   generated artifact — not source (gitignored)
│   └── v/cache/
├── .vscode/                         config only (gitignored) — 1 setting
│   └── settings.json
├── backend/                         ONLY PLACEHOLDERS + 1 near-empty module
│   ├── __init__.py                  empty (0 lines)
│   ├── Dockerfile                   real content
│   ├── .env                         present, gitignored (values not read)
│   ├── app/                         only __init__.py + an 8-line main.py
│   │   ├── __init__.py              empty (0 lines)
│   │   └── main.py                  8 lines, no routes
│   └── tests/                       EMPTY (only __init__.py)
│       └── __init__.py              empty (0 lines)
├── data/                            ONLY __init__.py (docstring)
│   ├── __init__.py                  4 lines, docstring only
│   └── instances/                   EMPTY (only .gitkeep)
│       └── .gitkeep
├── frontend/                        EMPTY (only .gitkeep) — not scaffolded
│   └── .gitkeep
└── solver/                          ONLY __init__.py (docstring) — NO ALGORITHM CODE
    ├── __init__.py                  5 lines, docstring only
    └── tests/                       EMPTY (only __init__.py)
        └── __init__.py              empty (0 lines)
```

Directories that **do not exist** at all: `alembic/`, `migrations/`, `.github/`, `data/generated/`,
`frontend/src/`, `backend/app/models/`, `backend/app/routers/`, `backend/app/schemas/`.

**Summary:** zero directories contain real application logic. The only files with more than a
docstring are `backend/app/main.py` (8 lines, creates a bare app object), `backend/Dockerfile`,
`docker-compose.yml`, `requirements.txt`, `.gitignore`, `.dockerignore`, `.env.example`, and the
four Markdown documents.

---

## 2. Python inventory

Every `.py` file in the repo (excluding `.venv/` and `__pycache__/`). There are **7**.

| File | Lines | What it actually does |
|---|---|---|
| `backend/app/main.py` | 8 | Module docstring, `from fastapi import FastAPI`, `app = FastAPI(title="Chronos API")`. **No routes, no middleware, no DB session, no auth.** |
| `backend/__init__.py` | 0 | **Empty.** Package marker only. |
| `backend/app/__init__.py` | 0 | **Empty.** Package marker only. |
| `backend/tests/__init__.py` | 0 | **Empty.** Package marker only. |
| `data/__init__.py` | 4 | **Docstring only** — no code. States that generated output is gitignored. |
| `solver/__init__.py` | 5 | **Docstring only** — no code. States the solver is pure Python and must import no web/DB frameworks. |
| `solver/tests/__init__.py` | 0 | **Empty.** Package marker only. |

**Flagged:** six of the seven files are empty or docstring-only. The seventh (`main.py`) contains
three lines of executable code. **No function, class, dataclass, or algorithm is defined anywhere
in the repository.**

### `requirements.txt` (verbatim)

```
# --- Solver core ---
networkx==3.3              # validation / baseline colouring comparison only

# --- Backend: API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# --- Backend: Database ---
sqlalchemy==2.0.35
alembic==1.13.3
psycopg2-binary==2.9.9     # PostgreSQL driver (sync)
# asyncpg==0.29.0           # uncomment if using async SQLAlchemy engine

# --- Backend: Auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.12   # form data support for auth endpoints

# --- Config / env ---
python-dotenv==1.0.1
pydantic-settings==2.5.2

# --- Data generation ---
faker==28.4.1

# --- Testing ---
pytest==8.3.3
pytest-cov==5.0.0
hypothesis==6.112.1
httpx==0.27.2               # test client for FastAPI endpoints
```

### Virtual environment

**A venv exists** at `.venv/`. `.venv/Scripts/python.exe --version` reports **Python 3.12.10**
(Windows). `pytest.exe` and `alembic.exe` are present in `.venv/Scripts/`. Every top-level
requirement is installed in `.venv/Lib/site-packages` — verified present: `networkx`, `fastapi`,
`uvicorn`, `pydantic`, `sqlalchemy` (2.0.35), `alembic` (1.13.3), `psycopg2`, `jose`, `passlib`,
`python_multipart`, `dotenv`, `pydantic_settings`, `faker`, `pytest`, `coverage`, `hypothesis`,
`httpx`.

---

## 3. Database state

- **Migrations:** none. There is no `alembic/` or `migrations/` directory, therefore **no
  `versions/` directory and zero migration files.** Nothing has ever been migrated from this repo.
- **SQLAlchemy models:** **none.** No `declarative_base`, no `Base`, no model class, no
  `__tablename__` anywhere in the repo. Zero tables are defined in code.
- **`alembic.ini`:** does **not exist.** Not configured.
- **`env.py`:** does **not exist.** Not configured.
- **`DATABASE_URL`:** referenced in exactly three non-secret places, none of them code:
  - `.env.example` — declared as a template key
  - `docker-compose.yml` — mentioned in a comment explaining that the backend reads it from `backend/.env`
  - `CLAUDE.md` — prose

  No Python file reads it. There is no engine, no session factory, and no connection code.
  (`backend/.env` exists on disk and defines the key; its value was not read or printed.)

**Net:** the database layer does not exist beyond a connection string sitting in an env file.

---

## 4. Backend state

### Routes

**Zero.** `backend/app/main.py` is reproduced in full below — this is the entire backend:

```python
"""FastAPI application entrypoint.

Run locally with:  uvicorn backend.app.main:app --reload
"""

from fastapi import FastAPI

app = FastAPI(title="Chronos API")
```

There is no `@app.get`, `@app.post`, `APIRouter`, or `include_router` anywhere. Starting uvicorn
would serve only the auto-generated `/docs` and `/openapi.json` for an empty schema.

### Auth

**Neither wired nor scaffolded — entirely absent.** `python-jose` and `passlib[bcrypt]` are
installed, and `SECRET_KEY` / `ACCESS_TOKEN_EXPIRE_MINUTES` (and, in `backend/.env`, `ALGORITHM`)
are declared as env keys. But there is no token creation, no verification, no password hashing, no
`OAuth2PasswordBearer`, no dependency, no user model, and no role enum. The only thing that exists
is the dependency install and three key names.

---

## 5. Frontend state

- **`package.json`:** does **not exist.** `frontend/` contains one file, `.gitkeep`, whose content
  is the scaffolding instruction: `npm create vite@latest . -- --template react-ts`.
- **Dependencies / devDependencies:** not applicable — there is no manifest.
- **Component files:** **none.** No `.ts`, `.tsx`, `.js`, `.jsx`, `.css`, or `index.html` exists
  anywhere in the repo.
- **Does it build?** **No.** There is no build script, no bundler config, no `tsconfig.json`, and
  no source. Every `npm` command in `CLAUDE.md` fails today. (Not run, per instructions — this is
  determined by the absence of a manifest.)

None of React, Vite, TypeScript, Tailwind, shadcn/ui, dnd-kit, TanStack Query, Recharts, or
Vitest is installed or configured.

---

## 6. Solver state

| File | Lines | Status |
|---|---|---|
| `solver/__init__.py` | 5 | **Stubbed** — module docstring only, zero code |
| `solver/tests/__init__.py` | 0 | **Stubbed** — empty package marker |

That is the complete contents of `solver/`. Consequently:

- Conflict-graph construction — **not implemented** (no file)
- Greedy colouring — **not implemented** (no file)
- Backtracking / forward checking / MRV — **not implemented** (no file)
- MUS extraction (infeasibility diagnosis) — **not implemented** (no file)
- Freeze-and-expand repair — **not implemented** (no file)
- Solver ↔ backend edge-list/JSON contract — **not defined in code** (prose only, in the Markdown docs)

### Purity check (project rule)

Searched `solver/` for imports of `fastapi`, `sqlalchemy`, `psycopg2`, and `alembic`:

**No violations found.** No such import exists. This is trivially true — `solver/` contains no
import statements at all. The rule is currently unviolated but also untested, because there is no
solver code to violate it.

---

## 7. Tests

- **Test files:** **none.** No file matching `test_*.py` or `*_test.py` exists. `solver/tests/` and
  `backend/tests/` each contain only an empty `__init__.py`.
- **Test functions:** **0**, across the whole repo.
- **pytest configured?** Installed (`pytest==8.3.3`, plus `pytest-cov` and `hypothesis`), but
  **not configured** — there is no `pytest.ini`, `pyproject.toml`, `setup.cfg`, `tox.ini`, or
  `conftest.py`. A `.pytest_cache/` directory exists, showing pytest has been invoked at some
  point; it collected nothing. (Not run for this report.)
- **vitest configured?** **No.** No `vitest.config.ts`, no `package.json`, no frontend at all.
- The Hypothesis property test that `CLAUDE.md` requires to always pass ("every generated timetable
  must be conflict-free") **does not exist.**

---

## 8. Config and docs

| Item | Exists | One-line summary |
|---|---|---|
| `CLAUDE.md` | Yes (121 lines) | Agent guidance: project overview, non-negotiables (no ML, solver purity), architecture, tech stack, conventions, commands, per-member module ownership. |
| `CONTEXT.md` | Yes (287 lines), **untracked** | Stable project charter — what Chronos is, the rules that must not break, and what the real institutional data looks like. |
| `CURRENT-PROGRESS.md` | Yes (265 lines), **untracked** | Living progress log; currently marked "Phase 0 — Foundation and contract freeze", last-updated field reads *(not yet started)*. |
| `ROADMAP.md` | Yes (384 lines), **untracked** | 12-week, 3-track (solver / backend / frontend) implementation plan with Must/Should/Could tagging. |
| `README` | **No** | Absent in every form (`README`, `README.md`, …). |
| `.gitignore` | Yes | Comprehensive: Python caches, venvs, test/coverage caches, `.env*` (un-ignoring `.env.example`), `data/generated/`, local scratch DBs, Node/Vite output, editor and OS files. |
| `.env.example` | Yes | Template for `backend/.env`. **Key names only:** `DATABASE_URL`, `SECRET_KEY`, `ACCESS_TOKEN_EXPIRE_MINUTES`. Comments explain Supabase URI format and that Alembic needs the direct connection (port 5432), not the 6543 pooler. |
| `backend/.env` | Yes (gitignored) | **Key names only:** `DATABASE_URL`, `SECRET_KEY`, `ALGORITHM`, `ACCESS_TOKEN_EXPIRE_MINUTES`, `ENVIRONMENT`, `DEBUG`. Values not read. Note `ALGORITHM`, `ENVIRONMENT`, and `DEBUG` are **not** in `.env.example` — the template is out of sync. |
| `.dockerignore` | Yes | Keeps `.venv/` (Windows binaries), caches, `.git/`, `node_modules/`, `.env`, and `*.md` out of the Linux build context. |
| `docker-compose.yml` | Yes | Single `backend` service built from `backend/Dockerfile` with repo root as context; reads `backend/.env`, publishes `8000:8000`, bind-mounts `.:/app`. **No `db` service** — it connects out to hosted Supabase. |
| `backend/Dockerfile` | Yes | `python:3.12-slim`, installs root `requirements.txt`, copies the repo, exposes 8000, runs `uvicorn backend.app.main:app --host 0.0.0.0 --port 8000 --reload`. |
| CI workflow | **No** | No `.github/` directory at all — no GitHub Actions, despite `CLAUDE.md` listing it in the stack. |
| `.vscode/settings.json` | Yes (gitignored) | One key: `python-envs.defaultEnvManager`. |

---

## 9. Git state

- **Current branch:** `main`
- **Total commits:** **2** (on `HEAD`)
- **All commits** (fewer than 10 exist):

  | SHA | Author | Subject |
  |---|---|---|
  | `9db89fb` | Nidhi Dhyani | chore: switch to Supabase, update .env config and CLAUDE.md |
  | `9fddff5` | Nidhi Dhyani | chore: repo scaffold, requirements.txt, .gitignore |

- **Working tree:** **not clean.** Three untracked files: `CONTEXT.md`, `CURRENT-PROGRESS.md`,
  `ROADMAP.md`. (This file, `CURRENT-STATUS.md`, will be a fourth.) No modified or staged files.
- **Local branches:** `main` only.
- **Remote branches:** `origin/main` only. Remote is
  `https://github.com/Nidzz07/Automated-Academic-Timetable-Scheduling-System.git`.

> Note: `CLAUDE.md` currently states "there are still no commits on `main`". That is out of date —
> two commits exist and are pushed.

---

## 10. Honest gap assessment

**This repository cannot generate a timetable. It cannot generate anything.** It is a directory
skeleton, a dependency list, a Dockerfile, and roughly 1,050 lines of planning prose. Total
executable code in the project: **three lines** (an import, an `app =` assignment, and that is it).

To produce a single timetable, a developer would still need to write, from nothing:

1. **The entire solver.** No conflict graph, no colouring, no backtracking, no MUS extraction, no
   repair heuristic — not even a `Session` or `Slot` dataclass. `solver/` is two files containing
   a docstring and nothing else. This is the graded artifact and it has not been started.
2. **A data model.** No SQLAlchemy models, no tables, no schema. Department, Division, Batch,
   Faculty, Room, Session, and Cohort exist only as nouns in the Markdown.
3. **The whole migration setup.** `alembic init` has never been run: no `alembic.ini`, no `env.py`,
   no `versions/`. The Supabase database that `DATABASE_URL` points at has, as far as this repo
   can show, no Chronos schema in it.
4. **Every API endpoint.** Zero routes exist. Nothing serves, accepts, or validates anything.
5. **All authentication and role logic.** The JWT libraries are installed; not one line uses them.
6. **The constraint-derivation layer.** Nothing turns database rows into the edge list the solver
   is supposed to consume — and the solver↔backend contract that `CLAUDE.md` calls a team handoff
   point exists only as prose, never as a schema or type.
7. **Input data.** `data/` has a docstring; the synthetic generator does not exist and
   `data/instances/` is empty. There is no instance to solve.
8. **The entire frontend.** Not scaffolded. No `package.json`, no source file, nothing to build.
9. **Every test.** Zero test functions. The mandatory Hypothesis conflict-free property test — the
   one `CLAUDE.md` says must always pass — has not been written, so "the solver suite is green" is
   currently vacuous.
10. **CI.** `CLAUDE.md` specifies GitHub Actions on push; `.github/` does not exist.

The honest one-line summary: **Phase 0 scaffolding is in place and correctly configured; feature
work has not begun.** Do not read the presence of `solver/`, `backend/app/`, or `data/` as evidence
that those components are partially built — each is an empty package with a comment inside it.

What *is* genuinely solid: the dependency pinning, the `.gitignore`/`.dockerignore` hygiene, the
Docker setup, the venv (Python 3.12.10, fully installed), and an unusually detailed set of planning
documents. That is real groundwork — it is just groundwork, not a system.
