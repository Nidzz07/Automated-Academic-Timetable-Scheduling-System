# --- Solver core ---
networkx==3.3              # validation / baseline colouring comparison only

# --- Ingestion ---
python-docx==1.1.2         # class / lab / faculty timetable .docx parsing
openpyxl==3.1.5            # classrooms.xlsx room inventory parsing
jsonschema==4.23.0         # validates payloads against the frozen contracts/

# --- Backend: API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# --- Backend: Database ---
sqlalchemy==2.0.35
alembic==1.13.3
psycopg2-binary==2.9.9     # PostgreSQL driver (sync)
# asyncpg==0.29.0           # uncomment if using async SQLAlchemy engine

# --- Backend: Auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.12   # form data support for auth endpoints

# --- Config / env ---
python-dotenv==1.0.1
pydantic-settings==2.5.2

# --- Data generation ---
faker==28.4.1

# --- Testing ---
pytest==8.3.3
pytest-cov==5.0.0
hypothesis==6.112.1
httpx==0.27.2               # test client for FastAPI endpoints

# --- Tooling ---
ruff==0.6.9                 # lint + format; runs in CI on every push
