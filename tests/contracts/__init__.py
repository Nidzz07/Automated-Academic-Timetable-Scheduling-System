"""Contract conformance tests.

Each test loads an example payload from contracts/examples/ and validates it
against its JSON Schema. These are the tests that stop the three tracks drifting
apart — see CONTEXT.md section 5.
"""
